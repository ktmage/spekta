import { describe, it } from "vitest";

// [spekta:summary] キーワードや条件で検索する機能。【テスト更新】
describe("検索", () => {
  describe("データが存在する場合", () => {
    it("キーワードで検索できる", () => {});
  });

  describe("データが存在しない場合", () => {
    it("該当なしと表示される", () => {});
  });
});
