import { describe, it } from "vitest";

// [spekta:page] 詳細ページ
// [spekta:summary] 詳細情報を確認するページ。
describe("詳細ページ", () => {
  // [spekta:section] データが存在する場合
  describe("データが存在する場合", () => {
    // [spekta:why] 基本情報はユーザーが最初に確認する項目であるため。
    // [spekta:section] 基本情報が表示される
    it("基本情報が表示される", () => {});
  });
});
