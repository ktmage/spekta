import { describe, it } from "vitest";

// [spekta:summary] 詳細情報を確認するページ。
describe("詳細ページ", () => {
  describe("データが存在する場合", () => {
    // [spekta:why] 基本情報はユーザーが最初に確認する項目であるため。
    it("基本情報が表示される", () => {});
  });
});
